import React, { FC } from "react";

const Header: FC = () => {
  return (
   <div className="flex items-center text-lg text-white justify-center bg-slate-500">Define Header</div>
    
  );
};

export default Header;
